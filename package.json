{
  "type": "module",
  "name": "backendnotes",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf dist && cd ../notes && npm run build && cp -r dist ../backendnotes",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev":"nodemon index.js"
  },
  "author": "shivani kumari",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
